{
  "name": "notify-caregiver-lambda",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin AWS Lambda relay that sends the real caregiver email alert via SES, so the browser never holds AWS credentials.",
  "dependencies": {
    "@aws-sdk/client-sesv2": "^3.658.0",
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0"
  }
}
